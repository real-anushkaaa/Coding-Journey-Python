# ----------------------------------------
# Create a lambda function to find smaller number.
# ----------------------------------------

smaller = lambda a, b: "a" if a<b else "b"

print(smaller(10,20))