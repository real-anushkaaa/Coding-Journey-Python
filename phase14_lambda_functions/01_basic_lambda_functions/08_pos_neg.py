# ----------------------------------------
# Create a lambda function to check positive or negative.
# ----------------------------------------

pos_neg = lambda a: "pos" if a>=0 else "neg"

print(pos_neg(-5))