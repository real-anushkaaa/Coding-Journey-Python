# ----------------------------------------
# Create lambda function to convert string into lowercase.
# ----------------------------------------

lower = lambda text: text.lower()

print(lower("PYTHON"))